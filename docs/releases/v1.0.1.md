# LazyVerilog v1.0.1 Release Notes

Bug-fix release.

## Fixes

- **Hover** — fixed hover returning incorrect results in some cases.
- **Client config** — added client-side configuration support.
- **Installer** — fixed multiple curl messages appearing during binary download.
