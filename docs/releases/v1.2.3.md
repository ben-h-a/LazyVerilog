# LazyVerilog v1.2.3 Release Notes

## Performance Improvement
- fixed bug: Every file edit triggered a reparse of all files in .f file.

## Rename and Move Linting Options
- move config `module_instantiation_style`, `stale_instance_diagnostic` to `[lint.instance]` from `[lint.module]`.
