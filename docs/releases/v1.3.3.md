# LazyVerilog v1.3.3 Release Notes

Go-to-definition accuracy fix.

## Fixes

- Go-to-definition now chooses the correct signal when a struct field and a module signal share the same name.
- Member access still resolves correctly, so `entry.valid` jumps to the `valid` field while plain `valid` jumps to the local signal.
